import 'package:flutter/material.dart';

class EscrowPaymentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Escrow Payment')));
}